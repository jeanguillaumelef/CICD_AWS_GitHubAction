// import { HelloWorldController } from './HelloWorldController';
// import { Request, Response } from 'express';

// test('Test HelloWorldController', () => {
//   const controller = new HelloWorldController();

//   // Mock request and response
//   const mockRequest = {} as Request;
//   const mockResponse = {
//     send: jest.fn(),
//     json: jest.fn(),
//   } as unknown as Response;

//   // Call the method with the mocked request and response
//   controller.helloWorld(mockRequest, mockResponse);

//   // Check if the response was 'Hello, World!'
//   expect(mockResponse.send).toHaveBeenCalledWith('Hello, World!');
// });